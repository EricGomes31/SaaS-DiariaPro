node_modules/
dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Zip de deploy (gerado por Compress-Archive a partir de dist/)
hostgator-upload.zip

# Supabase CLI — estado local
supabase/.temp/

# Playwright — relatórios/artefatos de execução local
playwright-report/
test-results/

# Claude Code — memória e config local do agente
.claude/projects/
.claude/settings.local.json

# Symlink de skill com caminho absoluto (máquina-específico) — o conteúdo
# real já está versionado em .agents/skills/
.claude/skills/
